# This file contains python only requirements to be installed with pip
# Python pacakges that cannot be installed with pip (e.g. PyQt5) are not included.
# Usage: pip install -r requirements.txt
numpy>=1.8
matplotlib>=1.3.1
shapely<2.0
simplejson
rtree
scipy
svg.path
vispy==0.7
